#include"bits/stdc++.h"
using namespace std;

int main() {
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL;

    int t;
    cin >> t;

    while(t--) {
        int x1, x2, y1, y2;
        cin >> x1 >> y1 >> x2 >> y2;

        
    }
}