#include"bits/stdc++.h"
using namespace std;

int main()
{
	ios_base :: sync_with_stdio(false);
	cin.tie(NULL);
	
	int t, n, d, p;
	cin >> t;
	while(t--)
	{
		cin >> n;
		char str[n+1];
		cin >> str;
		cin >> d;
		for(int i = 0; i < d; i++)
		{
			cin >> p
			
		}
	}
}
